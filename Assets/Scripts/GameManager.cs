using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void CalculateDamage(AI opponent, Player playerchar, Skill skill){
        // bool accVal = AccuracyValue(playerchar);
        // if (!accVal){
        //     didMoveHit = false;
        //     //type out the move missed, go on to ai attack...
        // }else{
        //     opponent.Hp = opponent.Hp - (skill.atkVal * playerchar.atk * opponent.def);
        // }

    }
}
